name: CD

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install Zola
      run: sudo snap install --edge zola
    - name: Build
      run: |
        zola build
